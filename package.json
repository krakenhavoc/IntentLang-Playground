{
  "private": true,
  "scripts": {
    "build:editor": "esbuild web/editor.js --bundle --format=esm --outfile=web/editor.bundle.js",
    "build": "npm run build:editor"
  },
  "dependencies": {
    "@codemirror/lang-json": "^6.0.1",
    "@codemirror/language": "^6.10.0",
    "@codemirror/state": "^6.5.4",
    "codemirror": "^6.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
